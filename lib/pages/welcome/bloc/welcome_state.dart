part of 'welcome_bloc.dart';

/// Represents the state for the WelcomeBloc.
class WelcomeState {
  /// The current page index in the Welcome screen.
  int page;

  /// Creates a new instance of WelcomeState with an optional initial [page].
  WelcomeState({this.page = 0});
}
